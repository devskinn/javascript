// Use constants to assign variables which will never change
const person = {
    age: 28
}

person.age = 29 // will work as only the object property is being changed
// person = {} - will fail as a constant cannot be re-assigned

